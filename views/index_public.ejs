<!DOCTYPE html>
<html lang="es">

<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>
		Interfaz Pública
	</title>
	
	<link rel="icon" type="image/x-icon" href="/images/favicon.ico">
	<link rel="stylesheet" type="text/css" href="/stylesheets/style interfaz.css">
</head>

<body>
	<div id="screen">
		<h1 style="text-align: center; font-weight: bold;">Lentes de Sol para Toda la Familia</h1>
		<h2 style="text-align: center; font-weight: normal;">Interfaz Pública</h2>
		<hr>
		<% if (prod.length > 0) { %>
		<% prod.forEach(function(prod) { %>
		<% img.forEach(function(img) { %>

		<img style="width: 20%; height: 20%;" src="<%= img.url %>">
		<p><%= prod.nombre %></p>
		<p><%= prod.color %></p>
		<p><%= prod.talla %></p>
		<p><%= prod.codigo %></p>
		<p><%= prod.precio %></p>
		<p><%= prod.descripcion %></p>
		<p><%= prod.categoria_id %></p>

		<% }); %>
		<% }); %>
		<% } else { %>
			<p style="text-align: center;">No hay productos que mostrar.</p>
		<% }; %>
	</div>
</body>

</html>